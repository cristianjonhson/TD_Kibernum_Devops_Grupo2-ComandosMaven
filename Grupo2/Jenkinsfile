def COLOR_MAP = [
  'SUCCESS': 'good',
  'FAILURE':'danger',
]

pipeline {
  agent any

  /*environment {
    MAVEN_HOME = tool 'Maven 3' // Define tu herramienta Maven configurada en Jenkins
    JAVA_HOME = tool 'jdk 17' // Define tu herramienta JDK configurada en Jenkins
    PATH = "${MAVEN_HOME}/bin:${JAVA_HOME}/bin:${env.PATH}"
  }*/

  stages {
    /*stage('Checkout') {
      steps {
        // Clonar el repositorio
        git url: 'https://github.com/cristianjonhson/TD_Kibernum_Devops_Grupo2-ComandosMaven.git', branch: 'master'
      }
    }*/

    stage('Build') {
      steps {
        // Construir el proyecto con Maven
        sh 'mvn -f Grupo2/pom.xml clean package'
      }
    }

    /*stage('Test') {
      steps {
        // Ejecutar pruebas con Maven
        sh 'mvn -f Grupo2/pom.xml test'
      }
      post {
        always {
          // Publicar resultados de pruebas
          junit '**/ /*target/surefire-reports/*.xml'
        }
      }
    }*/

    /*stage('Upload Artifact') {
      steps {
        // Ejecutar el script para desplegar el artefacto
        sh 'mvn deploy:deploy-file 
        -DgroupId=cl.TalentoDigitalKibernum \
        -DartifactId=Grupo2 \
        -Dversion=1.0.2 \
        -Dpackaging=jar \
        -Dfile=Grupo2/target/Grupo2-1.0-SNAPSHOT.jar \
        -DgeneratePom=true \
        -DaltDeploymentRepository=comandosmaven::http://localhost:8081/repository/comandosmaven/'
      }
      steps {
        // Ejecutar el script para desplegar el artefacto
        sh 'mvn deploy:deploy-file 
        -DgroupId=cl.TalentoDigitalKibernum \
        -DartifactId=Grupo2 \
        -Dversion=1.0.2 \
        -Dpackaging=jar \
        -Dfile=Grupo2/target/Grupo2-1.0-SNAPSHOT.jar \
        -DgeneratePom=true \
        -DrepositoryId=comandosmaven \
        -Durl=http://localhost:8081/repository/comandosmaven/'
      }
    }*/
  }
    
  /*post {
      always {
        // Limpieza de archivos temporales
        cleanWs()
      
    }
  }*/
  post {
    always{
      echo 'Slack Notification desde Jenkinsfile para Pruebas Unitarias'
      slackSend channel: 'jenkins',
      color: COLOR_MAP[currentBuild.currentResult],
      message: "Slack Notification desde Jenkinsfile para Pruebas Unitarias\n*${currentBuild.currentResult}: Job ${env.JOB_NAME} (jenkinsfile) - build ${env.BUILD_NUMBER}\n More Info at: ${env.BUILD_URL}"
    }
  }
}